function greetUser(){
    var firstName = document.forms["nameForm"].elements ["firstName"].value;
    placeholder = "John" >
    var lastName = document.forms (name.forms)["nameForm"].elements ["lastName"].value;
    placeholder = "Doe"
    
    alert("Your name is " + firstName + lastName);
}